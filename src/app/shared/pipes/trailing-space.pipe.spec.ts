import { TrailingSpacePipe } from './trailing-space.pipe';

describe('TrailingSpacePipe', () => {
  it('create an instance', () => {
    const pipe = new TrailingSpacePipe();
    expect(pipe).toBeTruthy();
  });
});
